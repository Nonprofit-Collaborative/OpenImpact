# C-17 Addresses: integration notes

Everything in this file touches a **shared** file that C-17 must not edit directly:
permission sets, the Account and Contact page layouts, and the Nonprofit Hub app. The
owning feature applies these entries. Nothing here changes the behavior of the address
feature itself; it changes who can see it and where it appears.

## 1. Permission set entries

Add to the packaged permission sets. No entry requires cloning a permission set (plan
Section 7.4).

### Nonprofit Admin, Fundraising Staff, Program Staff, Volunteer Coordinator

| Type | Name | Access |
|---|---|---|
| Object | `Address__c` | Create, Read, Edit, Delete, View All not required |
| Field | `Address__c.Account__c` | Read, Edit |
| Field | `Address__c.Contact__c` | Read, Edit |
| Field | `Address__c.Person_Account__c` | Read, Edit |
| Field | `Address__c.Type__c` | Read, Edit |
| Field | `Address__c.Street__c` | Read, Edit |
| Field | `Address__c.City__c` | Read, Edit |
| Field | `Address__c.State__c` | Read, Edit |
| Field | `Address__c.Postal_Code__c` | Read, Edit |
| Field | `Address__c.Country__c` | Read, Edit |
| Field | `Address__c.Seasonal_Start_Month__c` | Read, Edit |
| Field | `Address__c.Seasonal_Start_Day__c` | Read, Edit |
| Field | `Address__c.Seasonal_End_Month__c` | Read, Edit |
| Field | `Address__c.Seasonal_End_Day__c` | Read, Edit |
| Field | `Address__c.Is_Default__c` | Read, Edit |
| Field | `Address__c.Verification_Status__c` | Read, Edit |
| Field | `Address__c.Latest_Verified_Date__c` | Read, Edit |
| Tab | `Address__c` | Visible |
| Apex class | `AddressController` | Enabled |
| Apex class | `AddressService` | Enabled |
| Apex class | `AddressSelector` | Enabled |

`Verification_Status__c` and `Latest_Verified_Date__c` are editable so a third party
verification app running as one of these users can write them (R-AD6). Packaged code never
writes either field.

### Read Only

Same list, with **Read** on the object and on every field, no Create, Edit, or Delete, tab
visible, and `AddressController` enabled so the address panel renders. `setDefault` fails
for these users in user mode, which is correct: the panel reports it with
`Core_Address_SetDefaultFailed`.

### Nonprofit Settings

The Addresses section of the settings console is governed by the existing
`Manage_Nonprofit_Settings` custom permission. C-17 adds no new custom permission.

## 2. Page layout placements

### Account page layouts (Household and Organization)

- **Related list:** `Address__c` through the `Addresses` relationship (`Account__c`),
  labelled **Addresses**, columns: Address Number, Type, Street, City, State, Postal Code,
  Default. Sort by Default descending. Place it below the Household Members related list on
  the Household layout and below Contacts on the Organization layout.
- **Component:** the `addressPanel` Lightning web component on the record page, in the main
  column, directly under the highlights panel. The panel is the place a person changes an
  address; the related list is there for reporting and for bulk edits.
- **Second related list:** `Address__c` through the `Personal_Addresses` relationship
  (`Person_Account__c`), labelled **Personal Addresses**, added only on the Person Account
  layout in an org where person accounts are enabled. It is empty and harmless elsewhere.

### Contact page layout (Household Contact)

- **Related list:** `Address__c` through the `Addresses` relationship (`Contact__c`),
  labelled **Personal Addresses**, columns: Address Number, Type, Street, City, State,
  Postal Code, Default.
- **Component:** the `addressPanel` component in the main column under the highlights panel.

### Nonprofit Hub app

Add the **Addresses** tab (`Address__c`) to the Nonprofit Hub app's tab set, after
Organizations. Its list views ship with C-17: All Addresses, Addresses In Use, Seasonal
Addresses, Addresses Not Yet Verified.

## 3. Automation registry

C-17 ships three `Automation_Registry__mdt` records, all with `Execution_Order__c` 20, so
they run after the household naming handlers (order 10) have settled membership:

| Developer name | Object | Handler class |
|---|---|---|
| `Address_Default_Propagation` | `Address__c` | `AddressTriggerHandler` |
| `Contact_Address_Capture` | Contact | `ContactAddressHandler` |
| `Account_Address_Capture` | Account | `AccountAddressHandler` |

C-17 ships the `AddressTrigger` trigger on `Address__c`. It ships **no** Contact or Account
trigger: those exist already and dispatch by registry (ADR-0017).

## 4. Contracts assumed, to be confirmed against the real Core classes

These were stubbed offline because no org and no Core source were available in this
worktree. Each is a mechanical fix if the real signature differs; the address logic itself
does not change.

| Contract | Shape assumed |
|---|---|
| `TriggerHandler` | Interface with `beforeInsert`, `beforeUpdate`, `beforeDelete`, `afterInsert`, `afterUpdate`, `afterDelete`, `afterUndelete`. If Core defines it as a virtual base class instead, the three handlers need the `override` keyword and nothing else. |
| `TriggerDispatcher.run(String)` | Called from `AddressTrigger` with the literal `Address__c`. |
| `ErrorLogger.log(Exception, String)` | Exception first, context string second. |
| `SettingsService.getSettings()` | Returns `Nonprofit_Settings__c`. |
| `HouseholdService.getMemberPersonIds(Set<Id>)` | Returns `Map<Id, Set<Id>>` keyed by household id, values the member person ids, which are Contact ids in contact mode and may be Account ids where people are person accounts. |
| `HouseholdService.getHouseholdIdsFor(Set<Id>)` | Returns `Map<Id, Set<Id>>` keyed by person id, values the households that person currently belongs to. |
| `OrgShapeDetector.detect()` | Returns an object with `hasPersonAccounts` (Boolean) and `personAccountRecordTypeIds` (`Set<Id>`). |
| `TestDataFactory` | `newHousehold(String)`, `newOrganization(String)`, `newContact(String, String, Id)`, all returning records that are not yet inserted. |
| `Automation_Registry__mdt` | Fields `Handler_Class__c`, `Object__c`, `Execution_Order__c`, `Default_Enabled__c`, `Description__c`. |
| `Setting_Definition__mdt` | Fields `Settings_Object__c`, `Setting_Key__c`, `Section__c`, `Data_Type__c`, `Picklist_Values__c`, `Default_Value__c`, `Description__c`, `Display_Order__c`, `Help_Link__c`. |

## 5. One platform constraint worth an ADR

The canonical model types `Contact_Address_Change_Behavior__c` as a picklist, and a custom
setting field cannot be a picklist. C-17 ships it as Text(40) with the two values offered by
the settings console through the `Setting_Definition__mdt` record, and with the code
treating anything blank as "Update household". Several v0.1 keys have the same problem
(`Coexistence_Mode__c`, `Household_Membership_Mode__c`, `Rollup_Mode_Default__c`), so the
deviation belongs to the settings feature (C-03) rather than to this one: it should be
recorded once, as one ADR covering every picklist-typed setting key.

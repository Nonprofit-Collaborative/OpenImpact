<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
  <fullName>Exactly_One_Donor</fullName>
  <active>true</active>
  <description>
    R-G1: a gift has a donor contact or a donor account, never both and never neither.
  </description>
  <errorConditionFormula>
    OR(
  AND(ISBLANK(Donor_Contact__c), ISBLANK(Donor_Account__c)),
  AND(NOT(ISBLANK(Donor_Contact__c)), NOT(ISBLANK(Donor_Account__c)))
)
  </errorConditionFormula>
  <errorMessage>
    Enter a donor contact or a donor account, not both. A gift has exactly one donor.
  </errorMessage>
</ValidationRule>

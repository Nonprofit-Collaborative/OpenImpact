<?xml version="1.0" encoding="UTF-8" ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Balance__c</fullName>
    <description>Expected total less paid to date for a pledge; empty for a recurring commitment, which has no expected total.</description>
    <formula>IF(ISPICKVAL(Type__c, &quot;Pledge&quot;), BLANKVALUE(Expected_Total__c, 0) - BLANKVALUE(Paid_To_Date__c, 0), NULL)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Balance</label>
    <precision>16</precision>
    <required>false</required>
    <scale>2</scale>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>

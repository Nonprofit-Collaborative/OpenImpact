<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
  <fullName>Maximum_Above_Minimum</fullName>
  <active>true</active>
  <description>
    A rule whose maximum is at or below its minimum could never match a gift, so it is refused
    where it is typed rather than discovered when nobody is thanked.
  </description>
  <errorConditionFormula>
    NOT(ISBLANK(Maximum_Amount__c)) &amp;&amp; NOT(ISBLANK(Minimum_Amount__c)) &amp;&amp;
    Maximum_Amount__c &lt;= Minimum_Amount__c
  </errorConditionFormula>
  <errorDisplayField>Maximum_Amount__c</errorDisplayField>
  <errorMessage>
    The maximum amount has to be greater than the minimum amount. Leave the maximum empty for a
    rule with no upper bound.
  </errorMessage>
</ValidationRule>

<?xml version="1.0" encoding="UTF-8" ?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
  <fullName>One_Notification_Recipient_Record</fullName>
  <active>true</active>
  <description>
    The person to notify is one person, so at most one of the two recipient references is set
    (canonical model Section 4, person references).
  </description>
  <errorConditionFormula>
    AND( NOT(ISBLANK(Notification_Recipient_Contact__c)),
    NOT(ISBLANK(Notification_Recipient_Account__c)) )
  </errorConditionFormula>
  <errorMessage>Choose one person to notify, not two.</errorMessage>
</ValidationRule>

# G-08, G-09, G-10: what the integrator has to wire up

Soft credits (G-08), tributes (G-09), and matching gift linkage (G-10) ship their own objects,
Apex, and components, but four kinds of shared metadata belong to whoever integrates the
iteration, because they are merge conflict magnets and this branch deliberately does not touch
them (plan Section 9.2).

Everything below is a request, not a change.

## 1. Permission set entries

Three permission sets now exist in the Giving package itself: **Giving Admin**, **Giving
Staff**, and **Giving Read Only** (`packages/giving/main/default/permissionsets/`). These
features need the following added to them. The columns below were written against the Core
permission sets before the Giving ones landed; read Admin, Staff, and Read Only as the Giving
three, and add `Contact.Employer__c` and `Account.Employer__c` to the Core sets as well, since
those two fields sit on Core owned objects.

### Objects

| Object | Nonprofit Admin | Nonprofit Staff | Nonprofit Read Only |
|---|---|---|---|
| `Soft_Credit__c` | create, read, edit, delete | create, read, edit, delete | read |
| `Tribute__c` | create, read, edit, delete | create, read, edit, delete | read |

Staff need create and delete on Soft Credit, not only read: the package's own automation writes
those records in user mode (`AccessLevel.USER_MODE`), so a gift saved by someone without create
access on Soft Credit would fail rather than silently skip the household credit. That is
deliberate, and it is why the entry matters.

### Fields

All fields on both objects, readable and editable for Admin and Staff, readable for Read Only,
with two exceptions that are read only for everyone because the package maintains them:

- `Soft_Credit__c.Is_Automatic__c`
- `Soft_Credit__c.Percent__c` (writable in the API, shown read only on the layout)

New fields on objects Core and Giving already own:

| Field | Nonprofit Admin | Nonprofit Staff | Nonprofit Read Only |
|---|---|---|---|
| `Contact.Employer__c` | read, edit | read, edit | read |
| `Account.Employer__c` | read, edit | read, edit | read |
| `Gift__c.Tribute__c` | read (package maintained mirror) | read | read |
| `Gift__c.Matched_Gift__c` | read, edit | read, edit | read |

### Apex classes

Add to Admin and Staff: `SoftCreditController`, `TributeController`. The services, selectors, and
handlers do not need class access, because nothing outside the package calls them directly, and
neither does `HouseholdSoftCreditRecompute`: it runs as a queued job under the user who changed
the household membership, not through a class entry point.

### Tabs

`Soft_Credit__c` and `Tribute__c` tabs: Visible for Admin and Staff, Available for Read Only.

## 2. Related lists and components on the Gift layout

The Gift page layout is owned by the Giving objects feature (G-01). Two related lists and two
components belong on it:

| Placement | What | Columns or notes |
|---|---|---|
| Related list | Soft Credits | Contact, Account, Role, Amount, Is Automatic |
| Related list | Tributes | Type, Honoree Name, Notification Sent, Notification Sent Date |

The two layout fields and the two record page components are no longer a request: this branch
adds a **Recognition** section to `Gift__c-Gift Layout` carrying `Matched_Gift__c` (editable, the
way a match is linked, R-G11) and `Tribute__c` (read only, a mirror the package maintains, R-G8),
and ships `Gift_Record_Page.flexipage-meta.xml` with `giftSoftCredits` and `tributeNotification`
already on it. What the integrator still has to do is assign that page as the org default record
page for Gift, because Lightning page assignment is not something a package can set for a
subscriber. Both admin guide pages say so in their setup step. The two related lists above are
still a request against the layout.

On the Contact and Account layouts, `Employer__c` belongs in the same section as the
organization's other affiliation information.

## 3. Duplicate shipped records to reconcile

Nothing left to reconcile. `Setting_Definition.Giving_Automatic_Household_Soft_Credits` was
shipped by both this branch and the Giving objects feature; the merge with `main` kept that
feature's record and corrected its `Help_Path__c` to `admin-guide/soft-credits.md`, which is
where the setting is now documented.

## 4. Assumptions this branch made that the integrator should confirm

- **`Rollup_Definition_Default__mdt` field API names.** The six rollup defaults this feature
  needs are recorded in Section 6 below rather than shipped, because the metadata type does not
  exist yet. Their field names are taken from canonical model Section 13 and Section 14. If C-13
  names a field differently, Section 6 changes with it.
- **Target filter notation.** The household and account scoped rollups separate their target
  record sets with a filter on `RecordType.DeveloperName`, written in the filter document format
  of R-R2. If the engine expects another way of naming the record type, these records change with
  it.
- **`Giving_Settings__c` and the `Automatic_Household_Soft_Credits__c` key** are created by the
  Giving objects feature. This branch reads the key through
  `SettingsService.getValue('Giving_Settings__c', 'Automatic_Household_Soft_Credits__c')` and
  treats a missing value as on, so it degrades safely if the settings object arrives later.
- **`Gift__c` and its fields** (`Donor_Contact__c`, `Donor_Account__c`, `Amount__c`, `Status__c`,
  `Gift_Date__c`, `Type__c`) come from the same feature. The two lookups this branch adds to that
  object (`Tribute__c`, `Matched_Gift__c`) are separate files, so there is no file level conflict.
- **`GiftTrigger`** is shipped by the Giving objects feature and calls
  `TriggerDispatcher.run('Gift__c')`. This branch adds two registry rows for that object rather
  than touching the trigger: `GiftSoftCreditHandler` at execution order 30 and
  `GiftMatchingGiftHandler` at 40.

## 5. Automation registry rows shipped here

| Developer name | Object | Order | Handler |
|---|---|---|---|
| `Giving_Gift_Soft_Credits` | `Gift__c` | 30 | `GiftSoftCreditHandler` |
| `Giving_Gift_Matching_Gifts` | `Gift__c` | 40 | `GiftMatchingGiftHandler` |
| `Giving_Soft_Credit_Rules` | `Soft_Credit__c` | 10 | `SoftCreditTriggerHandler` |
| `Giving_Tribute_Rules` | `Tribute__c` | 10 | `TributeTriggerHandler` |
| `Giving_Household_Membership_Soft_Credits` | `Household_Member__c` | 30 | `HouseholdSoftCreditHandler` |
| `Giving_Contact_Household_Soft_Credits` | `Contact` | 60 | `HouseholdSoftCreditHandler` |

## 6. Rollup definition defaults this feature needs (not shipped here)

`Rollup_Definition_Default__mdt` does not exist yet: it belongs to the rollup adapter work
(C-13). Shipping a custom metadata record whose type is undefined refuses the whole deployment,
so the six soft credit rollup defaults G-08 needs are recorded here instead, to be created as
records by whoever builds that type.

Two things about them are decisions of this feature rather than of the rollup work, and must
survive the move:

- The source filter is `Gift__r.Status__c equals Received`, so a refunded or written off gift
  stops counting towards recognition the same way it stops counting towards giving (R-SC5).
  Section 26 of the canonical model carries the same filter.
- Both target fields are recognition attributes of their own. No packaged rollup ever adds a
  soft credit into a giving total (R-SC2), so a household member who is soft credited for a
  gift their spouse gave is never counted twice against the hard credit.

### Soft Credit Count (organizations and people stored as accounts)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Account_Soft_Credit_Count` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Account` |
| `Relationship_Path__c` | `Account__c` |
| `Aggregate__c` | `COUNT` |
| `Source_Field__c` | (empty) |
| `Target_Field__c` | `Soft_Credit_Count__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "RecordType.DeveloperName", "operator": "not equals", "value": "Household"}]}` |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

How many gifts this donor is recognized on without being hard credited.

### Total Soft Credits (organizations and people stored as accounts)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Account_Total_Soft_Credits` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Account` |
| `Relationship_Path__c` | `Account__c` |
| `Aggregate__c` | `SUM` |
| `Source_Field__c` | `Amount__c` |
| `Target_Field__c` | `Total_Soft_Credits__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "RecordType.DeveloperName", "operator": "not equals", "value": "Household"}]}` |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

The total this donor is recognized for without being hard credited. Soft credits are recognition, not money, so this number is never added into a giving total.

### Soft Credit Count (contacts)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Contact_Soft_Credit_Count` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Contact` |
| `Relationship_Path__c` | `Contact__c` |
| `Aggregate__c` | `COUNT` |
| `Source_Field__c` | (empty) |
| `Target_Field__c` | `Soft_Credit_Count__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | (empty) |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

How many gifts this donor is recognized on without being hard credited.

### Total Soft Credits (contacts)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Contact_Total_Soft_Credits` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Contact` |
| `Relationship_Path__c` | `Contact__c` |
| `Aggregate__c` | `SUM` |
| `Source_Field__c` | `Amount__c` |
| `Target_Field__c` | `Total_Soft_Credits__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | (empty) |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

The total this donor is recognized for without being hard credited. Soft credits are recognition, not money, so this number is never added into a giving total.

### Soft Credit Count (households)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Household_Soft_Credit_Count` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Account` |
| `Relationship_Path__c` | `Contact__c > {Household} | Account__c > {Household}` |
| `Aggregate__c` | `COUNT` |
| `Source_Field__c` | (empty) |
| `Target_Field__c` | `Soft_Credit_Count__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "RecordType.DeveloperName", "operator": "equals", "value": "Household"}]}` |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

How many gifts this donor is recognized on without being hard credited.

### Total Soft Credits (households)

| Field | Value |
|---|---|
| `Definition_Key__c` | `Household_Total_Soft_Credits` |
| `Source_Object__c` | `Soft_Credit__c` |
| `Target_Object__c` | `Account` |
| `Relationship_Path__c` | `Contact__c > {Household} | Account__c > {Household}` |
| `Aggregate__c` | `SUM` |
| `Source_Field__c` | `Amount__c` |
| `Target_Field__c` | `Total_Soft_Credits__c` |
| `Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "Gift__r.Status__c", "operator": "equals", "value": "Received"}]}` |
| `Target_Filter_JSON__c` | `{"version": 1, "conditions": [{"id": 1, "field": "RecordType.DeveloperName", "operator": "equals", "value": "Household"}]}` |
| `Fiscal_Year_Aware__c` | `false` |
| `Mode__c` | `Both` |
| `Default_Active__c` | `true` |
| `Package__c` | `Giving` |

The total this donor is recognized for without being hard credited. Soft credits are recognition, not money, so this number is never added into a giving total.

## Applied at merge

The Section 1 entries went onto the Giving package's three permission sets, not Core's:
`Employer__c` on Contact and on Account is shipped by `packages/giving`, so its field
permissions belong with the package that owns the field. Soft Credit and Tribute objects,
their fields, the two tabs, the two controllers, and the four fields on objects the
packages already own are all in place. The two package maintained fields
(`Soft_Credit__c.Is_Automatic__c`, `Soft_Credit__c.Percent__c`) and the Tribute mirror on
Gift are readable but not editable anywhere.

# Salesforce
.sf/
.sfdx/
.localdevserver/
deploy-options.json

# Node
node_modules
node_modules/
npm-debug.log*

# Coverage / test artifacts
coverage/
apexcov.json

# Logs
*.log

# Editor
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/

# OS
.DS_Store

# Local tool caches
# Matches the directory and any stray symlink of the same name: an agent worktree can
# leave one pointing outside the repo, which a "git add -A" would otherwise commit.
.tools

# Misc
/tmp

# Claude Code agent worktrees
.claude/worktrees/

# Output of scripts/org/run-org-tests.sh. Kept out of git because that script refuses a dirty
# tree, so its own results must not make the next run look dirty.
test-results/
